version: '2'

services:
  web:
    build: .
    command: pumactl -F /portus/config/puma.rb start
    environment:
      - PORTUS_MACHINE_FQDN_VALUE=${EXTERNAL_IP}
      - PORTUS_DB_HOST=db
      - PORTUS_DB_PASSWORD=portus
      - PORTUS_PUMA_HOST=0.0.0.0:3000
      - PORTUS_PUMA_WORKERS=2
      - PORTUS_PUMA_MAX_THREADS=4
    volumes:
      - .:/portus
    ports:
      - 3000:3000
    links:
      - db

  crono:
    image: portus_web
    entrypoint: bin/crono
    environment:
      - PORTUS_MACHINE_FQDN_VALUE=${EXTERNAL_IP}
      - PORTUS_DB_HOST=db
      - PORTUS_DB_PASSWORD=portus
    volumes:
      - .:/portus
    links:
      - db

  db:
    image: library/mariadb:10.0.23
    environment:
      MYSQL_ROOT_PASSWORD: portus

  registry:
    image: library/registry:2.3.1
    environment:
      - REGISTRY_AUTH_TOKEN_REALM=https://${EXTERNAL_IP}:443/v2/token
      - REGISTRY_AUTH_TOKEN_SERVICE=${EXTERNAL_IP}:${REGISTRY_PORT}
      - REGISTRY_AUTH_TOKEN_ISSUER=${EXTERNAL_IP}
    volumes:
      - /registry_data
      - ./compose/registry/portus.crt:/etc/docker/registry/portus.crt:ro
      - ./compose/registry/config.yml:/etc/docker/registry/config.yml:ro
    ports:
      - ${REGISTRY_PORT}:5000
      - 5001:5001 # required to access debug service
    links:
      - web
  portus-ssl:
    image: nginx
    environment:
      - NGINX_CERT_FILE: /etc/nginx/certs/star.bryonyportal.com.crt
      - NGINX_KEY_FILE: /etc/nginx/certs/star.bryonyportal.com.key
      - NGINX_DOMAIN: dhub.bryonyportal.com
    volumes:
      - ./nginx/proxy:/etc/nginx/conf.d
      - /etc/nginx/certs:/etc/nginx/certs
    ports:
      - 443:443
    links:
      - web
